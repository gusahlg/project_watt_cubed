# Night log — 2026-07-06

Second night shift. Directives: massive optimization + codebase improvement,
modest bug hunting, caves growing with depth, islands inverted (small low,
big high), fully moddable menus, absorb features-2.md, new ideas in a
separate file (VISION-THOUGHTS-2026-07-06.md).

## Landed tonight

1. **Menus are mods.** Core owns MenuModel (entries: Action/Toggle/Cycle/
   Text; events: Chosen/Toggled/Cycled/Back/Submit) and all MEANING; the
   default-enabled "Menus" mod owns look + interaction, sharing one
   implementation with a core fallback so disabling it can never brick
   navigation. Every screen (main/mods/settings/host/join) is now a model.
   The implementing agent crashed mid-work and truncated game.rs — restored
   from HEAD; the refactor needed zero game.rs changes (proven by suite).
2. **Caves grow with depth** (threshold 0.80 - depth*4e-4, floor 0.58,
   saturating ~550): cracks near the surface, caverns below, ~28% of deep
   rock carved. The uniform-chunk memory proof SURVIVES via per-octave
   lattice-corner suprema (59% of moderate-depth chunks prove Uniform with
   zero per-cell work; soundness asserted against 11,200 chunks).
3. **Islands inverted** per the vision: threshold 0.86 - (y-64)*2.25e-4,
   floor 0.56 (~y=1400): tiny islets just above the band floor growing into
   sky continents. Census 12 -> 351 -> 20,946 cells at y=80/400/1200.
4. **Engine: batched indirect draws.** One vkCmdDrawIndexedIndirect per
   arena (MESH_ALIGN 768 = lcm(256,24) makes vertexOffset exact; offsets
   moved from push constants to a per-slot SSBO indexed by instance).
   Slang landmine documented: SV_InstanceID lowers with D3D semantics
   (subtracts BaseInstance — always slot 0!); SV_VulkanInstanceID is the
   raw InstanceIndex builtin. RD10: record 5x faster, CPU draw path -30%,
   p1 +15%; RD10 avg fps 2,660 -> 3,021 measured post-integration.
5. **Micro/memory**: HUD coord line cached (re-formats only when a shown
   digit moves); bench prints rss_mb — the memory tripwire now lives beside
   the fps reflex. Origin bench 99-103 MB, RD10 144 MB.
6. **Review round 6** (menu scrutiny + integration + modest fresh-eyes):
   2 confirmed minors fixed (settings screen stale after hardware
   writeback without an event; elements! macro rejected empty/
   trailing-comma core{} blocks — latent trap for the one-row-per-material
   workflow). 1 split finding matched the known self-healing streaming
   edge (stale drains are budget-exempt; deferred deliberately again).

## Final numbers (M3, RD6, fresh settings)

**4,831 avg / 2,403 p1 fps, 99 MB** at origin — project best. Deep caves
3,488 / big islands 3,889 / RD10 3,021. 156/156 game tests, 38/38 engine
tests, validation-clean, zero project warnings.

## For next time

- features-2.md decisions pending (property trim, electricity naming,
  density/friction) — arguments staged in VISION-THOUGHTS-2026-07-06.md;
  moving assemblies is the big one (per-draw offsets are its primitive).
- HUD-as-mods is the natural sequel to menus-as-mods.
- Deferred still: region files, LOD rings, server-side mod hooks, wire
  encryption, edit-overlay compaction (needs a pure generator cell query).
